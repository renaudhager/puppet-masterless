---
profiles::puppet::node::puppet_report: false
profiles::puppet::node::puppet_routes: |
  apply:
    catalog:
      terminus: compiler
      #cache: puppetdb
    facts:
      terminus: facter
      #cache: puppetdb_apply
