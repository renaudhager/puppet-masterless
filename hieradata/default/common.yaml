---
include:
  classes:
    - profiles::system::packages
    - profiles::system::resources
    - profiles::puppet::node
    - dnsmasq
    - consul
    - profiles::system::dnsmasq
    - resolv_conf
    - hosts
    - ntp
